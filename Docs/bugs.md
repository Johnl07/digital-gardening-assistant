bugs i see on dark mode
1. some text blend with the bg 
    homepage:
        -hello john at the top left seems to blend with the bg
        -crop distribution: crop names
        -daily garden tip description
    add palnt page:
        -select plant: bg of plants is white, should be dark
        -ploanting date text blends in the bg
        -current stage: same as above white bg
        -select season: same as above white bg
        -sunlight: same as above white bg + text blends
        -water: same as above white bg + text blends
        -soil: same as above white bg + text blends
    garden page: inside each plant:
        - stage and health blends in the bg
        - log daily care action is in light mode, like it should be dark mode but it is white
        